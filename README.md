# Calculator App

A recreation of the **iOS Calculator app**, built in **Swift using Xcode**.

The project replicates the core functionality and interface of the standard iOS calculator while serving as a project to practice **Swift and iOS app development**.
